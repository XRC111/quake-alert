# Gradle / Kotlin daemon
org.gradle.jvmargs=-Xmx4096M -Dfile.encoding=UTF-8 -Dkotlin.daemon.jvm.options\="-Xmx4096M"
org.gradle.parallel=true
org.gradle.caching=true
# KGP 2.1.20 + Gradle 8.11.1 + configuration cache 在 Kotlin/Native cinterop 任务上
# 仍有 classpathSnapshot 反序列化错误（参见 KTIJ-32914），先关闭以稳定 CI
org.gradle.configuration-cache=false

# Kotlin
kotlin.code.style=official
# KGP 2.4：androidSourceSetLayoutVersion 已废弃（V2 默认开启不可改，KT-82265），已移除
# iOS framework 需要静态链接以便 Xcode 集成
kotlin.native.binary.objcExportSuspendFunctionLaunchThreadRestriction=none

# Android
android.useAndroidX=true
android.nonTransitiveRClass=true
android.defaults.buildfeatures.buildconfig=false
